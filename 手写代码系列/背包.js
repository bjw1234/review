// 一组物品，每种物品都有自己的重量和价格。
// 在限定的总重量内，如何选择才能使得物品的总价格最高。

/**
 * @param {*} weight 物品重量
 * @param {*} value 物品价格
 * @param {*} countWeight 总重量
 */
function knapsack(weight, value, countWeight) {
  let array = new Array(length).fill(
    new Array(C + 1).fill(null)
  )

  for (let i = 0; i <= countWeight; i++) {
    
  }
}
